services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: labuser
      POSTGRES_PASSWORD: labpassword
      POSTGRES_DB: labdb
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

  app:
    build: ./app
    depends_on:
      - db
    environment:
      DATABASE_URL: "postgresql://labuser:labpassword@db:5432/labdb"
      # Enable auto-reload inside the container (change to "false" to disable)
      AUTO_RELOAD: "true"
      # Default script to run inside the container (can be "main.py" or "algorithms.py")
      APP_SCRIPT: "main.py"
    volumes:
      - ./app:/usr/src/app
    # Use the dev runner which will pick up APP_SCRIPT and AUTO_RELOAD
    command: ["sh", "/usr/src/app/dev_runner.sh"]
    ports:
      - "8000:8000" # if you want to expose any web service

  pgadmin:
    image: dpage/pgadmin4:7
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.local
      PGADMIN_DEFAULT_PASSWORD: admin
    depends_on:
      - db
    ports:
      - "8080:80"
    volumes:
      - pgadmin-data:/var/lib/pgadmin

  frontend:
    build: ./frontend
    depends_on:
      - app
    volumes:
      - ./frontend:/app
      - frontend-node-modules:/app/node_modules
    ports:
      - "5173:5173"
    environment:
      # Vite needs to bind to 0.0.0.0; package.json dev script already does that
      CHOKIDAR_USEPOLLING: "true"
    command: ["npm", "run", "dev"]

volumes:
  db-data:
  pgadmin-data:
  frontend-node-modules:
